# frozen_string_literal: true
require 'rspec'

RSpec.describe FS_DCAT_Proxy do
  it "has a version number" do
    expect(FS_DCAT_Proxy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
